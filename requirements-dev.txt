# Development / packaging dependencies (players DON'T need these — see requirements.txt).
#   pip install -r requirements-dev.txt
#   pyinstaller meridian_sea.spec          # desktop build
#   python -m pygbag --build .             # experimental WebAssembly build (spike)
-r requirements.txt
pyinstaller==6.20.0
pygbag==0.9.3
